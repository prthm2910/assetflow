from django.apps import AppConfig


class SearchConfig(AppConfig):
    """Configuration for the global search app (Module 13)."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.platform.search"
